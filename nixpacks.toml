# Frontend — Railway root directory must be / (repo root, NOT backend)
# Use npm install (not npm ci): lock file optional deps differ on Linux vs Windows
[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install --include=optional"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
