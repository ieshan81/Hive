# Backend secrets — copy to .env locally. NEVER commit .env to git.
# Production secrets live in Railway environment variables.

ALPACA_API_KEY=
ALPACA_SECRET_KEY=
ALPACA_BASE_URL=https://paper-api.alpaca.markets
GEMINI_API_KEY=
# Google AI model IDs — update when a version is deprecated (see https://ai.google.dev/gemini-api/docs/models)
GEMINI_MODEL=gemini-3.5-flash
GEMINI_MODEL_DEEP=
DATABASE_URL=sqlite:///./hive.db
RAILWAY_API_KEY=
OPERATOR_SECRET=

CORS_ORIGINS=http://localhost:3000
