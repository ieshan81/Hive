# Backend — Railway root directory must be /backend
# Do NOT override [phases.install]: Nixpacks auto-runs pip install -r requirements.txt
[variables]
NIXPACKS_PYTHON_VERSION = "3.11"

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
